---
interface Props {
  class?: string;
}

const { class: className = '' } = Astro.props;
---

<article class={`rounded-2xl border border-primary/10 bg-background-light shadow-sm transition-all ${className}`}>
  <slot />
</article>
